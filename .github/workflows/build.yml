name: Multi-Platform Build

concurrency:
  group: build-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-android:
    runs-on: ubuntu-22.04
    env:
      PUB_CACHE: ${{ github.workspace }}/.pub-cache
      GRADLE_USER_HOME: ${{ github.workspace }}/.gradle
    
    steps:
      - name: Runner Started
        run: echo "Runner acquired, starting Android build"

      # 1️⃣ Checkout the repo
      - uses: actions/checkout@v4

      # 2️⃣ Setup Java 17 (required for Android builds)
      - uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      # 3️⃣ Setup Flutter (using master channel for Dart 3.6+)
      - uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Flutter Version
        run: flutter --version

      - name: Flutter Doctor
        run: flutter doctor -v

      # 4️⃣ Disable analytics
      - run: flutter config --no-analytics

      # 5️⃣ Cache Flutter Pub packages
      - name: Cache Flutter Pub
        uses: actions/cache@v4
        with:
          path: ${{ env.PUB_CACHE }}
          key: ${{ runner.os }}-pub-${{ hashFiles('**/pubspec.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pub-

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: ${{ env.GRADLE_USER_HOME }}
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties', '**/settings.gradle*') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      # 6️⃣ Install dependencies
      - run: flutter pub get

      # 7️⃣ Build Android APK
      - name: Build Android APK
        timeout-minutes: 30
        run: flutter build apk --release --build-number ${{ github.run_number }}

      # 8️⃣ Upload APK artifact
      - name: Upload Android APK
        uses: actions/upload-artifact@v4
        with:
          name: android-apk-${{ github.run_number }}
          path: build/app/outputs/flutter-apk/*.apk

  build-windows:
    needs: build-android
    runs-on: windows-2022
    env:
      PUB_CACHE: ${{ github.workspace }}/.pub-cache
      GRADLE_USER_HOME: ${{ github.workspace }}/.gradle
    
    steps:
      - name: Runner Started
        run: echo "Runner acquired, starting Windows build"

      # 1️⃣ Checkout the repo
      - uses: actions/checkout@v4

      # 2️⃣ Setup Flutter (using master channel for Dart 3.6+)
      - uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Flutter Version
        run: flutter --version

      - name: Flutter Doctor
        run: flutter doctor -v

      # 3️⃣ Disable analytics
      - run: flutter config --no-analytics

      # 4️⃣ Cache Flutter Pub packages
      - name: Cache Flutter Pub
        uses: actions/cache@v4
        with:
          path: ${{ env.PUB_CACHE }}
          key: ${{ runner.os }}-pub-${{ hashFiles('**/pubspec.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pub-

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: ${{ env.GRADLE_USER_HOME }}
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties', '**/settings.gradle*') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      # 5️⃣ Install dependencies
      - run: flutter pub get

      # 6️⃣ Build Windows EXE
      - name: Build Windows EXE
        timeout-minutes: 45
        run: flutter build windows --release --build-number ${{ github.run_number }}

      # 6.5️⃣ Zip Windows build output
      - name: Zip Windows Build
        shell: pwsh
        run: |
          Compress-Archive -Path build/windows/x64/runner/Release/* -DestinationPath build/windows/x64/musicly_windows_x64_${{ github.run_number }}.zip

      # 7️⃣ Install Inno Setup
      - name: Install Inno Setup
        run: choco install innosetup -y

      # 8️⃣ Create Installer
      - name: Create Installer
        shell: pwsh
        run: |
          & "${env:ProgramFiles(x86)}\Inno Setup 6\ISCC.exe" installer_script.iss

      # 9️⃣ Upload Windows Installer
      - name: Upload Windows Installer
        uses: actions/upload-artifact@v4
        with:
          name: windows-installer-${{ github.run_number }}
          path: Output/*.exe

      # 9.5️⃣ Upload Windows EXE (raw)
      - name: Upload Windows EXE
        uses: actions/upload-artifact@v4
        with:
          name: windows-exe-${{ github.run_number }}
          path: build/windows/x64/runner/Release/*.exe

      # 🔟 Upload Windows Zip
      - name: Upload Windows Zip
        uses: actions/upload-artifact@v4
        with:
          name: windows-zip-${{ github.run_number }}
          path: build/windows/x64/musicly_windows_x64_${{ github.run_number }}.zip